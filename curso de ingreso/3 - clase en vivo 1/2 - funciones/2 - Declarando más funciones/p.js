/* Declarando más funciones
Entonces para escribir una función hay cosas que no podemos dejar pasar.

La palabra function, el nombre de la función, los parámetros si se necesitan, el código entre {} y un return dentro del código si queremos que nos devuelva algo.

Veamos si se va entendiendo: escribí ahora una función con nombre  mitad, que tome un número y devuelva su mitad.

--------------------------------------------------------------------------------------------------------------------------------------------------------
 */
// this file is read only, you cannot write it

function mitad(numero){
   return numero/2;
};

let n1 = 22;


console.log("la mitad de " + n1  + " es: " + mitad(n1));